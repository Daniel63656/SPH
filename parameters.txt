# Settings file for numsim program
# Run ./numsim lid_driven_cavity.txt

# constants
kappa = 2.0                     # gas constant
rho0 = 0.5
mass = 0.5                       # mass of the individual particle
mu = 1                          # kinematic viscosity of the fluid
particleDensity = 3
numberOfParticles = 121        # number of particles used in the simulation


# domain
physicalSizeX = 12.0             # physical size of the domain
physicalSizeY = 12.0
nCellsX = 1                   # number of cells in all spacial directions
nCellsY = 1
boundaryDensityX = 20
boundaryDensityY = 20
boundaryThickness = 2

# time
endTime = 10.0
dt = 0.01
vs_dt = 0.5

# misc
gX = 0.0                        # external forces, set to (gX,gY) = (0,-9.81) to account for gravity
gY = 0.0
kernelFunction = Gaussian       # specify which kernel function gets used
smoothness = 0.5                # specify kernels smoothing term
