# Settings file for numsim program
# Run ./numsim lid_driven_cavity.txt

# constants
kappa = 2.0                     # gas constant
rho0 = 2.0
mass = 10                       # mass of the individual particle
mu = 9                          # kinematic viscosity of the fluid
particleDensity = 5
numberOfParticles = 121        # number of particles used in the simulation


# domain
physicalSizeX = 2.0             # physical size of the domain
physicalSizeY = 2.0
nCellsX = 1                   # number of cells in all spacial directions
nCellsY = 1
boundaryDensityX = 20
boundaryDensityY = 20
boundaryThickness = 10

# time
endTime = 1.0
dt = 0.001
vs_dt = 0.001

# misc
gX = 0.0                        # external forces, set to (gX,gY) = (0,-9.81) to account for gravity
gY = -9.81
kernelFunction = Gaussian       # specify which kernel function gets used
smoothness = 5                # specify kernels smoothing term
